<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Bookmark Manager</title>
  </head>
  <body>
    <% if @user%>
      <h1>Welcome, <%= @user.email %></h1>
    <% end %>
    <ul>
      <% @bookmarks.each do |bookmark| %>
      <li> <a href="<%= bookmark.url%>"><%= bookmark.title%></a>
        <form action="/bookmarks/<%= bookmark.id %>" method="post">
        <div class="delete">
            <input id="hidden" type="hidden" name="_method" value="DELETE">
            <input type="submit" value="delete <%= bookmark.id %>">
        </div>
        </form>
        <form class="edit" action="/bookmarks/edit/<%= bookmark.id%>" method="GET">
              <input type="submit" value="edit">
        </form>
        <div>
          <form class="box" action="/bookmarks/<%= bookmark.id %>/comment/new" method="get">
              <input type="submit" value="comment">
          </form>
        </div>
        <div>
          <form class="tag" action="/bookmarks/<%= bookmark.id %>/tag/new" method="get">
            <input type="submit" value="add tag">
          </form>
        </div>
        <div>
          <h2>Comments</h2>
          <ul>
            <% bookmark.comments.each do |comment|%>
            <li><%= comment.text %></li>
            <% end %>
          </ul>
        </div>
        <div>
          <h2>Tags</h2>
          <ul>
            <% bookmark.tags.each do |tag|%>
            <li><%= tag.tag %></li>
            <%end%>
          </ul>
        </div>
        <% end %>
      </li>
    </ul>
      <h2><%= flash[:notice] %></h2>
  </body>
</html>
